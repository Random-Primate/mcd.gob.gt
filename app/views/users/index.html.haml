.container
  - breadcrumb :users
  .well
    .row
      %h1.col-md-6
        = 'Usuarios'
      .col-md-6
        = link_to 'Agregar Usuario', new_user_registration_path, class: 'btn btn-default pull-right title-btn'
  %br

  %table.table.table-striped.table-hover

    %tr
      %th= 'NOMBRE'
      %th= 'CORREO'
      %th{colspan: 3}
    - @users.each do |user|
      %tr
        %td= full_name(user)
        %td= user.email
        %td= link_to 'Editar', edit_user_registration_path(user.id)

  = will_paginate @users, class: 'paginate-bot'