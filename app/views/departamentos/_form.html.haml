= form_for @departamento do |f|
  - if @departamento.errors.any?
    #error_explanation
      %h2= "#{pluralize(@departamento.errors.count, "error")} prohibited this departamento from being saved:"
      %ul
        - @departamento.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
