require "rails_helper"

RSpec.describe SolicitedsController, :type => :routing do
  describe "routing" do
=begin
    it "routes to #index" do
      expect(:get => "/soliciteds").to route_to("soliciteds#index")
    end

    it "routes to #new" do
      expect(:get => "/soliciteds/new").to route_to("soliciteds#new")
    end

    it "routes to #show" do
      expect(:get => "/soliciteds/1").to route_to("soliciteds#show", :id => "1")
    end

    it "routes to #edit" do
      expect(:get => "/soliciteds/1/edit").to route_to("soliciteds#edit", :id => "1")
    end

    it "routes to #create" do
      expect(:post => "/soliciteds").to route_to("soliciteds#create")
    end

    it "routes to #update" do
      expect(:put => "/soliciteds/1").to route_to("soliciteds#update", :id => "1")
    end

    it "routes to #destroy" do
      expect(:delete => "/soliciteds/1").to route_to("soliciteds#destroy", :id => "1")
    end
=end
  end
end
